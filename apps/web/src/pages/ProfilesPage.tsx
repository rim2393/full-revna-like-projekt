import { placeholderSpecs } from '../shared/data/lumenData'
import { PlaceholderPage } from './PlaceholderPage'

export function ProfilesPage() {
  return <PlaceholderPage spec={placeholderSpecs.profiles} />
}
